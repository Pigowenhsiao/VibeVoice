<Window x:Class="VibeVoice.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="VibeVoice Desktop"
        Width="1320"
        Height="920"
        MinWidth="1080"
        MinHeight="760"
        Background="#0C1217"
        FontFamily="Bahnschrift">
    <Window.Resources>
        <SolidColorBrush x:Key="CardBrush" Color="#13212B" />
        <SolidColorBrush x:Key="LineBrush" Color="#263947" />
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,0,16" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EAF3F8" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#07131A" />
            <Setter Property="Background" Value="#7EE0C3" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="Margin" Value="0,0,12,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0F1A21" />
            <Setter Property="Foreground" Value="#EAF3F8" />
            <Setter Property="BorderBrush" Value="#335062" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0F1A21" />
            <Setter Property="Foreground" Value="#EAF3F8" />
            <Setter Property="BorderBrush" Value="#335062" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#081015" Offset="0" />
                <GradientStop Color="#102733" Offset="0.55" />
                <GradientStop Color="#0A1A22" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="28">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" CornerRadius="28" Padding="28" Margin="0,0,0,24">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#123445" Offset="0" />
                            <GradientStop Color="#1D5B64" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="VibeVoice Desktop" FontSize="34" FontWeight="Bold" />
                            <TextBlock Margin="0,10,0,0"
                                       Foreground="#D1EFF3"
                                       Text="Windows-native shell for local multi-speaker generation. Load the model, run the job, then play or export the result." />
                        </StackPanel>
                        <Button Grid.Column="1"
                                Content="Refresh Runtime"
                                Command="{Binding RefreshCommand}"
                                Background="#E7FFF8"
                                Margin="16,0,0,0"
                                VerticalAlignment="Center" />
                    </Grid>
                </Border>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="420" />
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="01 Load Model" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Margin="0,12,0,0" Text="{Binding RuntimeStatus}" />
                                <TextBlock Margin="0,8,0,0" Foreground="#A9C0CE" Text="{Binding RuntimeSummary}" TextWrapping="Wrap" />
                                <TextBlock Margin="0,16,0,6" Foreground="#A9C0CE" Text="Checkpoint Path" />
                                <TextBox Text="{Binding ModelPath, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Content="Load Model"
                                        Command="{Binding LoadModelCommand}"
                                        Margin="0,14,0,0" />
                                <TextBlock Margin="0,12,0,0" Text="{Binding ModelStatus}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="02 Speaker Presets" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Margin="0,12,0,0" Foreground="#A9C0CE" Text="Speaker 1" />
                                <ComboBox ItemsSource="{Binding Voices}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedVoice1}" />
                                <TextBlock Margin="0,12,0,0" Foreground="#A9C0CE" Text="Speaker 2" />
                                <ComboBox ItemsSource="{Binding Voices}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedVoice2}" />
                                <TextBlock Margin="0,12,0,0" Foreground="#A9C0CE" Text="Speaker 3" />
                                <ComboBox ItemsSource="{Binding Voices}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedVoice3}" />
                                <TextBlock Margin="0,12,0,0" Foreground="#A9C0CE" Text="Speaker 4" />
                                <ComboBox ItemsSource="{Binding Voices}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedVoice4}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="03 Generation Workspace" FontSize="22" FontWeight="SemiBold" />
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.2*" />
                                        <ColumnDefinition Width="24" />
                                        <ColumnDefinition Width="1*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Foreground="#A9C0CE" Text="Speaker Count" />
                                        <TextBox Text="{Binding SpeakerCount}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Foreground="#A9C0CE" Text="CFG Scale" />
                                        <Slider Minimum="1.0" Maximum="2.0" Value="{Binding CfgScale}" TickFrequency="0.1" />
                                        <TextBlock Margin="0,6,0,0" Text="{Binding CfgScale, StringFormat=F2}" />
                                    </StackPanel>
                                </Grid>

                                <TextBlock Margin="0,18,0,6" Foreground="#A9C0CE" Text="Script" />
                                <TextBox Height="280"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto"
                                         Text="{Binding ScriptText, UpdateSourceTrigger=PropertyChanged}" />

                                <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                                    <Button Content="Generate Job" Command="{Binding GenerateCommand}" />
                                    <Button Content="Stop Job" Command="{Binding StopCommand}" Background="#F6C17A" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="04 Job Progress" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Margin="0,12,0,0" Text="{Binding JobState}" FontSize="18" FontWeight="SemiBold" />
                                <ProgressBar Margin="0,12,0,0" Height="16" Minimum="0" Maximum="1" Value="{Binding JobProgress}" />
                                <TextBlock Margin="0,10,0,0" Text="{Binding JobMessage}" TextWrapping="Wrap" />
                                <TextBlock Margin="0,8,0,0" Foreground="#A9C0CE" Text="{Binding ElapsedTime, StringFormat=Elapsed Time: {0}}" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="05 Result / Export" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Margin="0,12,0,0" Text="{Binding ResultSummary}" TextWrapping="Wrap" />
                                <TextBlock Margin="0,10,0,0" Foreground="#A9C0CE" Text="Result Artifact" />
                                <TextBox Text="{Binding ArtifactPath}" IsReadOnly="True" />
                                <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                    <Button Content="Play Result" Command="{Binding PlayResultCommand}" />
                                    <Button Content="Stop Playback" Command="{Binding StopPlaybackCommand}" Background="#D9E2E8" />
                                    <Button Content="Export Copy" Command="{Binding ExportResultCommand}" Background="#F6C17A" />
                                </StackPanel>
                                <TextBlock Margin="0,12,0,0" Foreground="#A9C0CE" Text="{Binding PlaybackStatus}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Settings" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Margin="0,12,0,0" Foreground="#A9C0CE" Text="Output Directory" />
                                <TextBox Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock Margin="0,12,0,0" Foreground="#A9C0CE" Text="Runtime Diagnostics" />
                                <TextBlock Margin="0,8,0,0" Text="{Binding RuntimeSummary}" TextWrapping="Wrap" />
                                <Button Content="Save Current Defaults"
                                        Command="{Binding SaveSettingsCommand}"
                                        Margin="0,14,0,0" />
                                <TextBlock Margin="0,12,0,0" Text="{Binding SettingsStatus}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Errors" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Margin="0,12,0,0" Foreground="#FFB3B3" Text="{Binding ErrorMessage}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
